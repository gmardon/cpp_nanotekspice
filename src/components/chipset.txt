A implémenter:
 -4094 : 8bits shift register.

 -4514 : 4bits decoder.

 -4801 : Random access memory.

 -2716 : Read only memory. 2716 must takes a value when declared. In the .chipsets:
  section, you must precise a file that will contain the data written in the ROM: 2716
  rom(cp_m.dat)

Maj avec des vectors:
  -4001
